---
name: novel-writing
description: 用于起点、番茄风格的男频商业网文创作。适用于剧情讨论、设定与角色设计、卷纲和章纲、章节草稿、战斗与情感场景、连续性维护、经济体系、去除 AI 写作痕迹及阶段性审查。
---

# 男频商业网文创作

## 定位与作者权限

面向起点、番茄式男频商业网文，可适配玄幻、都市、科幻、悬疑、西幻、无限流等题材，不套用固定题材模板。

本 Skill 只能创作或修改草稿。章节写完、自检完成或审查结果良好，都不能使其自动成为正式正文。只有作者明确表示“归为正文”“列为正文”“正式采用”等，才改变章节状态并同步正式资料。

“归为正文”默认属于快速正式化事务，不属于重新审稿、深度同步或 Agent 复核。除非作者明确要求全面检查，否则不得因为正式化而扩大任务范围。

## 先判断工作状态

按 [workflow-and-status.md](references/workflow-and-status.md) 区分任务与授权；该文件统一规定确认、正式化与阶段收束的触发，其他参考文件不得额外叠加流程。作者已确定的方向及必要实现步骤直接执行，只对尚未授权且会显著改变故事的选择请求决定。讨论不落盘，草稿不污染正式连续性。

| 请求 | 工作范围 |
|---|---|
| 链接、状态、字数 | 定向读取后回答，不进入创作流程 |
| 归为正文 | 快速提交及有效增量合并，零子 Agent |
| 局部修改 | 定向改写与承接检查 |
| 普通章节 | 复用有效角色信息，必要时咨询；每章一次独立读者复核 |
| 关键战斗、告白、反转 | 相关角色集中咨询，存在明确疑点时针对性复核；每章一次独立读者复核 |
| 剧情段收束 | 对该段审查一次，已审范围只复核后续变动 |

参考文件按任务选读；同一上下文中已经读过且未变动的规则不重复加载。不同文件引用同项检查时只执行一轮。

## 分层读取上下文

所有剧情讨论、大纲、写作、修改和审查任务都先按 [context-layers.md](references/context-layers.md) 建立隐藏的 L0—L4 上下文：

- L0 当前任务与授权范围。
- L1 正式规则、角色状态及四表中的相关硬事实。
- L2 当前章节、相邻章节和关键场景原文。
- L3 远场摘要、人物经历、卷纲、伏笔和剧情状态。
- L4 本次排除的未来内容、废案、无关资料及角色不知道的信息。

只加载完成本次任务所需的最小上下文。默认读取相关规则、角色现状、前章收尾和直接相关的历史场景；影响本次对白、情感、动作或伏笔判断的原文不能只用摘要替代。当前选择已有充分依据时停止扩展读取。

资料冲突时依次采用：用户当前明确要求、已发布正文、最新正式设定、未发布的最新正式正文、已确认大纲、草稿、历史废案。可按优先级和明确版本解决时直接采用，注明受影响记录；是否实际修改记录仍按当前授权。只有同等有效来源冲突，或发布状态不明会影响当前关键判断时才询问。发布状态在初始化时记录，不在每章重复确认。

## 四表与经济体系

长篇项目使用 [continuity-ledgers.md](references/continuity-ledgers.md) 的四表管理时间、重要道具、正式术语和关键数字。它们是客观状态索引，不替代角色卡、角色经历和剧情连续性。

首次进入缺少经济基础的项目时，按 [economy-and-resources.md](references/economy-and-resources.md) 确认货币名称、换算、最低单位及是否可分割、人民币购买力锚。生产力沿用世界观或一并讨论；普通物价、工资和风险报酬据此推导，写成草稿待确认值，无需先确定整套经济。基础未定时继续不依赖经济数字的部分。已有资料只补当前必要缺项，不重复询问。

草稿完成后生成可直接合并的待确认状态变化；作者确认正式化后，以该记录为事务日志，只把本章实际变化合并进对应四表、经济记录、角色经历和正式连续性，不从全部项目资料重新推导一遍。

## 新角色分类与角色子 Agent

初始化时说明 S、A、B、C 四类及“逐个确认／允许暂定”偏好，默认推荐允许暂定。按 [character-agent-system.md](references/character-agent-system.md) 将作者已有描述直接映射分类；允许暂定时，普通功能角色暂列 C，剧情段重要角色暂列 B，随草稿告知。新核心主角、长期感情角色、主要反派的定位不明确时再询问。S、A、B 保持各自独立的角色视角，C 不创建 Agent。

角色 Agent 采用“正式状态版本＋草稿分支”隔离，调用调度与复用以 [character-agent-system.md](references/character-agent-system.md) 为唯一依据。正常推进和草稿原样采用允许下次发送确认增量后复用；删除经历、改变关键动机或切换不兼容分支才重建。正式化时只记录版本和兼容性，不调用 Agent。

重要场景需要新角色判断时，按角色合并对白、战斗、情感问题，一章同一角色默认咨询一次；已有有效判断可复用。主 Agent 完成场景后，只有出现具体人物失真或重大未决选择，才对受影响角色追加一次针对性复核。角色意见不直接拼接成台词，主 Agent 负责人物知识边界、整体因果与最终成稿。

在支持模型选择的 Codex 环境中，角色 Agent默认使用 `gpt-5.6-terra`，通常采用 `medium`，简单次要角色可用 `low`。创建时优先使用 `fork_turns="none"`，由主 Agent发送经过 L0—L3 筛选的角色上下文，避免复制完整对话。Terra不可用时使用当前环境兼顾能力和成本的模型。最终剧情与正文始终由主 Agent决定。

## 独立读者 Agent

每章新草稿或整章重写完成、主 Agent完成首次连续性检查和 AI 痕迹清理后，交付作者前必须按 [reader-agent-review.md](references/reader-agent-review.md) 创建一个独立读者 Agent。读者 Agent不扮演角色，也不读取大纲、场景卡、作者意图、角色卡、未来剧情或主 Agent的自检结论；它只从目标男频网文读者视角阅读完整本章及必要的前章收尾，重点寻找删去后没有损失的句子、作者替读者总结、故作深沉的比喻与金句、设定或道具台账侵入正文，以及不回应现场、不符合人物身份或过度完整的 AI 味对白。

读者 Agent只提交带原文定位的诊断和删除／改写方向，不整章代写。主 Agent必须逐条判断，直接修正不会改变核心含义的明确问题；涉及伏笔、人物动机、能力规则或结构的建议不得机械采用。读者复核不是质量门，不改变草稿状态。普通局部字词修改不强制重建读者 Agent，除非改动覆盖完整场景、关键对白或作者明确要求复查。

## 章节草稿流程

1. 建立 L0—L4 上下文并提取相关四表切片。
2. 按角色系统判断复用还是集中咨询，不因普通出场自动调用。
3. 在内部按 [plot-and-scene-design.md](references/plot-and-scene-design.md) 建立隐藏场景卡。
4. 按 [commercial-web-fiction.md](references/commercial-web-fiction.md) 保证主角主动性、推进、爽点和追读动力。
5. 对话与人物塑造按 [dialogue-and-characterization.md](references/dialogue-and-characterization.md) 建立交流意图与自然反应；该文件统一维护人话检查标准。
6. 战斗读取 [combat-writing.md](references/combat-writing.md)；重要情感读取 [prose-and-emotion.md](references/prose-and-emotion.md)。能力描述含糊时采用最保守且已被正式资料证明的效果，不得自行扩张精度、范围、对象、持续时间或附加效果。
7. 完稿后按 [draft-review-and-ai-cleanup.md](references/draft-review-and-ai-cleanup.md) 检查连续性与 AI 痕迹，并修正明确的草稿小问题。
8. 创建独立读者 Agent复核本章，处理其指出的明确 AI 味句子与 AI 味对白，再做一次局部承接检查。
9. 生成待确认状态变化，不更新正式资料。

隐藏场景卡、角色 Agent与读者 Agent的内部意见和检查过程默认不展示，除非作者要求。

## 阶段性审查

按工作状态文件在剧情段收束时自动审查一次，检查主角主动性、爽点、节奏、人物、逻辑、情绪、战斗与追读动力。单纯归为正文、查链接或统计字数时只记录待审事项；下次规划或创作时处理。保存已审范围及版本，避免反复全文审查。

审查只给问题、证据和修改方案，不判定通过或失败，不阻断创作，不擅自结构性重写，也不改变正文状态。

## 交付

- 讨论只给方案，除非作者要求记录。
- 章节创作和修改默认交付草稿，并给出可点击文件路径与改动摘要。
- 不在回复中重复粘贴整章，除非作者要求。
- 使用正常网文分段，不在每个自然段之间额外插入整行空白。
