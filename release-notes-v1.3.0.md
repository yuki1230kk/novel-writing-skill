# v1.3.0：快速正式化

这次更新重新整理了章节“归为正文”时的执行边界。正式化现在默认是一笔轻量、可追踪的状态提交，不会自动扩展成一次全项目复审。

## 主要变化

- 不调用角色或读者子 Agent。
- 不重新审稿或清理 AI 痕迹。
- 不扫描全部正文和设定文件。
- 直接合并已有的“待确认状态变化”。
- 只更新本章实际影响的资料。
- 角色 Agent 改为下次真正使用时再按最新正式状态初始化。
- 对白样本、全表校正、跨章连续性与长期伏笔整理，延后到剧情段或整卷的深度同步。

## 为什么这样改

旧流程容易把一句“归为正文”放大成角色 Agent 重建、读者复核、全表扫描和对白样本整理，既增加等待和 token 消耗，也可能重复执行草稿交付前已经完成的检查。

v1.3.0 将草稿审查与正式状态提交彻底分开：草稿完成时负责质量检查，作者确认后只提交已经记录的实际变化。需要全面复核时，作者仍可明确要求“全面同步”“深度检查”或“正式化并全面审查”。

## 规则一致性

本次同步修改了入口规则、正式化流程、连续性台账、角色 Agent 和读者 Agent 五处说明，避免触发条件互相冲突。

完整变化请查看 [CHANGELOG.md](CHANGELOG.md)。
